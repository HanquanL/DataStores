com.liu.datasource.username = admin
com.liu.datasource.password = lhq51100991!

#update your application.properties to always initialize the datasource
# and set it to create tables on startup
spring.datasources.initialization-mode = Always
spring.jpa.hibernate.ddl-auto = create

#set the properties to show and format sql.
spring.jpa.show-sql= true
spring.jpa.properties.hibernate.format_sql = true